/*
Type Qualifier:
- a keyword that is applied to a type, resulting in a qualified type

const

volatile
- an object may be changed by something external to the program at any time
and so must be re-read from memory every time it is accessed
- it allows to outside CPU to change a volatile variable
- it prevents the compiler’s optimization
 */
int main(){

  return 0;
}

