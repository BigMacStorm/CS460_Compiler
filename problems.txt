Declarations:
  struct/union - create scopes and insert elements
  enum
  typedef
  tricky types combinations
  invalid type handling

Git rid of scope test case
Endlines on error messages
Clean script file
Actual directions for running (can't run script)
